import React from 'react';
import ReactDOM from 'react-dom/client';
import CDOSimulator from './CDOSimulator';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CDOSimulator />
  </React.StrictMode>
);
